import { styled } from '@mui/material/styles';
export const CenterPage = styled("div")(({theme}) => ({
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    minHeight: "90vh",
    overflow: "auto"
}))
